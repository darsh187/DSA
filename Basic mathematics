#include <bits/stdc++.h>
using namespace std;

int counting(int n)
{
    int count = 0;
    while(n > 0)
    {
        count = count + 1;
        n = n / 10;
    }
    return count;
}

int main()
{
    int m;
    cin >> m;
    int x = counting(m);
    cout << x;
    return 0;
}
